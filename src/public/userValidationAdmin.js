/* export async function userValidationAdmin(req, res, next){
    if(req.session.email === ("adminCoder@coder.com")) {
        next()
    } else {
        return res.render("sessionExpired")
    }
} */